# Generative MIDI Sequencer — ユーザー・スタートアップガイド

本ガイドでは、4チャンネル・ジェネレーティブMIDIシーケンサーの操作方法とパラメータについて解説します。ハードウェア配線は [PINOUT.md](PINOUT.md) を、ソフトウェア設計の詳細は [ARCHITECTURE.md](ARCHITECTURE.md) を参照してください。

---

## 1. クイック・スタート（ファームウェア書き込み）

1. **BOOTSELモードで起動**: Pico の「BOOTSEL」ボタンを押しながら USB でPCと接続。
2. **`RPI-RP2` ドライブが出現**: エクスプローラーにポータブルドライブとして表示されます。
3. **書き込み**: `generative_midi_sequencer.uf2` をドライブにドラッグ＆ドロップ → 自動再起動してシーケンサーが起動します。

---

## 2. UI 画面構成

画面は **ヘッダーエリア** と **4行のトラックグリッド** で構成されています。

```
┌─────────────────────────────────────────────────────────────────┐
│ BPM [120]   [▶ RUN]   [SCL:MINOR]   [💾]                        │
├─────────────────────────────────────────────────────────────────┤
│       SPD  LEN  DEN  SHF  MUT  PRB  GAT  ROT  SCL  RND         │
│ CH1   x4   16   04   00   00   FL   50   36   S2   RN           │
│ CH2   x4   16   05   02   25   75   50   60   S3   RN           │
│ CH3   x8   16   08   03   00   85   50   72   S1   RN           │
│ CH4   /2   16   03   00   00   90   50   48   S4   RN           │
└─────────────────────────────────────────────────────────────────┘
```

- **ヘッダー**: BPM値（拍ごとにネガポジ反転フラッシュ）、再生状態、現在のスケール名、セーブ状態を表示。
- **トラック行**: 10個のパラメータセルを横一列に表示。選択中のセルはシアン枠線でハイライト。
- **ステップレーン**: 各トラック行の下部にユークリッドパターンと再生ヘッドをリアルタイム表示。

---

## 3. 操作方法

### 基本ナビゲーション

| 操作 | 動作 |
|:--|:--|
| **UP / DOWN** | カーソルをトラック行間で移動 |
| **LEFT / RIGHT** | カーソルをパラメータ列間で移動 |
| **A** | 選択パラメータ値を増加 |
| **B** | 選択パラメータ値を減少 |
| **LT (Shift) + A** | パラメータ値を高速増加（パラメータ依存） |
| **LT (Shift) + B** | パラメータ値を高速減少（パラメータ依存） |

### 特殊コマンド

| 操作 | 動作 |
|:--|:--|
| **A + B 同時押し**（MUT 選択中） | Mutation を即座に `0` にリセット（フレーズ固定） |
| **A + B 同時押し**（DEN 選択中） | Density を Length の半分に設定 |
| **PLAY** | シーケンス全体の 再生 / 停止 切り替え |
| **LT + UP** | マスターBPMを +1（上限 250） |
| **LT + DOWN** | マスターBPMを -1（下限 40） |
| **RT** | 選択中のトラックをミュート / ミュート解除 |
| **LT + PLAY** | 全トラック設定をフラッシュへ保存 |

---

## 4. パラメータ詳細

### 4.1. パラメータ一覧

各トラックの10列パラメータの詳細です。

| 列 | 表示 | パラメータ名 | 操作範囲 | 説明 |
|:-:|:-:|:--|:--|:--|
| 0 | **SPD** | 再生速度 (Speed) | `x1`〜`x8` / `/2` | マスタークロックに対するトラック個別の分周比。`x8`が最速、`/2`が最遅。 |
| 1 | **LEN** | ステップ長 (Length) | `1`〜`32` | シーケンスループのステップ数。変拍子でポリリズムを生成できる。A/B で±1、LT+A/B で±10。 |
| 2 | **DEN** | トリガー密度 (Density) | `0`〜`LEN` | Euclidean アルゴリズムが配置する発音パルス数。A/B で±1、LT+A/B で±10。`A+B`で Length の半分に即設定。 |
| 3 | **SHF** | ユークリッドシフト (Shift) | `0`〜`32` | リズムパターンの開始位置をローテーション。A/B で±1、LT+A/B で±10。 |
| 4 | **MUT** | 突然変異率 (Mutation) | `0, 25, 50, 75, 99` | Turing Machine のビット変異確率。25%刻みでスナップ。`A+B`で即0リセット。 |
| 5 | **PRB** | 発音確率 (Probability) | `0, 25, 50, 75, FL` | Euclidean ヒット時の発音確率。25%刻みでスナップ。`FL`(100%)は常に発音。低いほど「抜け感」が出る。 |
| 6 | **GAT** | ゲート音長 (Gate) | `SL, 25, 50, 75, 99` | 音の持続時間の割合。25%刻みでスナップ。`SL`はスタッカート/レガートをランダム切替。 |
| 7 | **ROT** | ルート音 (Root Note) | `0`〜`127` | ピッチ量子化の基準音（MIDIノート番号）。**A/B でオクターブ単位（±12）**で移動。LT+A/B で半音（±1）ファインチューン。 |
| 8 | **SCL** | スケール (Scale) | `S1`〜`S9` | 量子化スケール。A/B で9種を巡回。 |
| 9 | **RND** | ランダム突然変異 | (トリガー) | A または B で押すたびに、そのトラックのリズム・ピッチ・速度・確率パラメータを音楽的に全自動ランダム生成。 |

### 4.2. スケール一覧 (SCL)

| # | 表示 | スケール名 | 音楽的特徴 |
|:-:|:-:|:--|:--|
| S1 | `CHROM` | Chromatic | 全半音。自由なクロマチック探索。 |
| S2 | `MINOR` | Natural Minor | 定番のナチュラルマイナー。感情的な深み。 |
| S3 | `PHRYG` | Phrygian | ♭2の暗さ。フラメンコ・テクノ的。 |
| S4 | `DORIN` | Dorian | ♮6のマイナー。ジャズ・ディープテクノ。 |
| S5 | `PENTA` | Minor Pentatonic | 5音。ブルーノート、最も使いやすい。 |
| S6 | `HUNG` | Hungarian Minor | 増4度の独特な緊張感。エキゾチック。 |
| S7 | `WHOLE` | Whole Tone | 全音音階。無重力・夢幻的浮遊感。 |
| S8 | `BLUES` | Blues | ♭5ブルーノート追加のマイナーペンタ。 |
| S9 | `MAJP` | Major Pentatonic | 明るい5音。アジア的開放感。 |

### 4.3. 自動ベロシティ・ダイナミクス

設定不要の自動機能です。Turing Machine の内部シフトレジスタ値から動的にベロシティを生成します。

- **ベースレンジ**: 50〜113（Turing Machineの raw_cv に連動）
- **ダウンビート（Step 0）**: +14 ブースト → 最大 127
- **クォーターノート位置**: +7 ブースト → 最大 118
- **MUT=0 時**: ベロシティも固定パターンを繰り返す → 自然なアクセントが「鍵」のようにはまる
- **MUT が高い時**: ベロシティも有機的に変化 → 表情豊かなダイナミクス

### 4.4. RND（ランダム突然変異）の詳細

`RND` セルで A または B を押すと以下をランダム生成します：

1. **LEN**: `8, 12, 16, 24, 32` ステップからランダム選択
2. **DEN**: `2` 〜 `Length/2` のランダムな密度
3. **SHF**: `0` 〜 `Length-1` のランダムシフト
4. **MUT**: `0`（パターンを固定してから聴く）
5. **PRB**: `70`〜`100%` のランダムな発音確率
6. **GAT**: `30`〜`90%` のランダムなゲート長（15% の確率で SL モード）
7. **SPD**: `x1, x2, x3, x4, x6, x8, /2` からランダム選択
8. **Turing Machine シード**: 完全ランダムなシード値で音列を刷新

---

## 5. セーブ / ロード

- **保存**: `LT + PLAY` → ヘッダー右端のディスクアイコンがマゼンタ点滅（書込中）→ シアン点灯（保存完了）
- **ロード**: 電源投入時に自動でフラッシュから読み込み。データが存在しない場合はデフォルト値を使用。

> フラッシュ書き込み中は Core 1（MIDI エンジン）が一時停止しますが、Pico SDK の Multicore Lockout API により完全に安全に処理されます。

---

## 6. トラブルシューティング

| 症状 | 対処 |
|:--|:--|
| 画面が白または黒のまま | SCK (GP18)、MOSI (GP19) の接続を確認。LCDバックライト電源を確認。 |
| MIDIシンセから音が出ない | GP0 の結線と、シンセ側の受信MIDIチャンネル（Track 1=Ch1〜Track 4=Ch4）を確認。 |
| 起動時に音が全く出ない | フラッシュの旧セーブデータで PRB=0（発音確率0%）が読み込まれた可能性あり。`LT+PLAY` でいったん上書き保存するか、PRB パラメータを確認して調整してください。 |
| Shift+PLAY 後にフリーズする | 最新ファームウェアに更新してください。最新版ではマルチコア・ロックアウト処理により安全に動作します。 |
